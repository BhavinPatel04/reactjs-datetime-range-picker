export enum DatetimeRangeType {
  daily = "daily",
  weekly = "weekly",
  monthly = "monthly",
  quarterly = "quarterly",
  yearly = "yearly",
}

export enum InputFocusBlur {
  focus = 1,
  blur = 2,
}
